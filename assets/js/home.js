(function () {
  /* Home-specific JS — kept as a placeholder.
     The hero background animation is now a holographic fx scene
     injected by assets/js/components.js (.ic-fx-scene).
     The count-up animation for data-count metrics lives in shared.js. */
})();
